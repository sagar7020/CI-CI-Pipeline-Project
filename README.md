# CI-CI-Pipeline-Project
About This project is a CI/CD pipeline that automates the build and deployment of a Java application to a Kubernetes cluster using Terraform and Jenkins on Google Cloud Platform. The pipeline stages are defined in a Jenkinsfile and can be customized as needed, helping to speed up the software development lifecycle.
